<h1>All Contestants</h1>

<% @contestants.each do |contestant| %>
  <section id= "contestant-<%= contestant.id%>">
    <h4>Name: <%= contestant.name %></h4>
    <p>
      Projects:
      <% contestant.projects.each do |project| %>
        <%= project.name %>
      <% end  %>
    </p>
    <br>
  </section>

<% end %>
